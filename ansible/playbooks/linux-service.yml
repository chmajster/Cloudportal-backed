- name: Manage Linux service
  hosts: all
  become: true
  gather_facts: false
  tasks:
    - name: Apply requested service state
      ansible.builtin.service:
        name: "{{ service_name }}"
        state: "{{ service_state }}"
        enabled: "{{ service_enabled == 'true' }}"
