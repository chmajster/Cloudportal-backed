- name: Reboot Linux VM
  hosts: all
  become: true
  gather_facts: false
  tasks:
    - name: Reboot host and wait for it to return
      ansible.builtin.reboot:
        reboot_timeout: 900
        connect_timeout: 10
